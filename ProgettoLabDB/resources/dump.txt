CREATE TABLE  "ANGER" 
   (	"WORD" VARCHAR2(20) NOT NULL ENABLE, 
	"EMOSN" NUMBER(1,0), 
	"NRC" NUMBER(1,0), 
	"SENTISENSE" NUMBER(1,0), 
	"FREQUENCY_DATASET" NUMBER, 
	"LEXICAL_RES_FREQUENCY" FLOAT(126), 
	 CONSTRAINT "CHECK_EMOSN" CHECK (EmoSN BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "CHECK_NRC" CHECK (NRC BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "CHECK_SENT" CHECK (sentisense BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "WORD_PK" PRIMARY KEY ("WORD") ENABLE
   ) ;CREATE TABLE  "ANTICIPATION" 
   (	"WORD" VARCHAR2(20) NOT NULL ENABLE, 
	"NRC" NUMBER(1,0), 
	"SENTISENSE" NUMBER(1,0), 
	"FREQUENCY_DATASET" NUMBER, 
	"LEXICAL_RES_FREQUENCY" FLOAT(126), 
	 CONSTRAINT "CHECK_NRC_ANT" CHECK (NRC BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "CHECK_SENT_ANT" CHECK (sentisense BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "WORD_PK_ANT" PRIMARY KEY ("WORD") ENABLE
   ) ;CREATE TABLE  "CONSCORE" 
   (	"WORD" VARCHAR2(20) NOT NULL ENABLE, 
	"AFINN" NUMBER(1,0), 
	"ANEWARO" FLOAT(126), 
	"ANEWDOM" FLOAT(126), 
	"ANEWPLEAS" FLOAT(126), 
	"DAL_ACTIV" FLOAT(126), 
	"DAL_IMAG" FLOAT(126), 
	"DAL_PLEAS" FLOAT(126), 
	"FREQUENCY_DATASET" NUMBER, 
	"LEXICAL_RES_FREQUENCY" FLOAT(126), 
	 CONSTRAINT "CHECK_AFINN" CHECK (afinn BETWEEN -5 and 5) ENABLE, 
	 CONSTRAINT "CHECK_ANEWARO" CHECK (anewAro BETWEEN 1 and 9) ENABLE, 
	 CONSTRAINT "CHECK_ANEWDOM" CHECK (anewDom BETWEEN 1 and 9) ENABLE, 
	 CONSTRAINT "CHECK_ANEWPLEAS" CHECK (anewPleas BETWEEN 1 and 9) ENABLE, 
	 CONSTRAINT "WORD_PK_CON" PRIMARY KEY ("WORD") ENABLE
   ) ;CREATE TABLE  "DISGUST_HATE" 
   (	"WORD" VARCHAR2(20) NOT NULL ENABLE, 
	"NRC" NUMBER(1,0), 
	"SENTISENSE_DISGUST" NUMBER(1,0), 
	"SENTISENSE_HATE" NUMBER(1,0), 
	"FREQUENCY_DATASET" NUMBER, 
	"LEXICAL_RES_FREQUENCY" FLOAT(126), 
	 CONSTRAINT "CHECK_NRC_DISHAT" CHECK (NRC BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "CHECK_SENT_DIS" CHECK (sentisense_disgust BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "CHECK_SENT_HAT" CHECK (sentisense_hate BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "WORD_PK_DISHAT" PRIMARY KEY ("WORD") ENABLE
   ) ;CREATE TABLE  "EMP" 
   (	"EMPNO" NUMBER(4,0) NOT NULL ENABLE, 
	"ENAME" VARCHAR2(10), 
	"JOB" VARCHAR2(9), 
	"MGR" NUMBER(4,0), 
	"HIREDATE" DATE, 
	"SAL" NUMBER(7,2), 
	"COMM" NUMBER(7,2), 
	"DEPTNO" NUMBER(2,0), 
	 PRIMARY KEY ("EMPNO") ENABLE
   ) ;CREATE TABLE  "FEAR" 
   (	"WORD" VARCHAR2(20) NOT NULL ENABLE, 
	"NRC" NUMBER(1,0), 
	"SENTISENSE" NUMBER(1,0), 
	"FREQUENCY_DATASET" NUMBER, 
	"LEXICAL_RES_FREQUENCY" FLOAT(126), 
	 CONSTRAINT "CHECK_NRC_FEAR" CHECK (NRC BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "CHECK_SENT_FEAR" CHECK (sentisense BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "WORD_PK_FEAR" PRIMARY KEY ("WORD") ENABLE
   ) ;CREATE TABLE  "HOPE" 
   (	"WORD" VARCHAR2(20) NOT NULL ENABLE, 
	"SENTISENSE" NUMBER(1,0), 
	"FREQUENCY_DATASET" NUMBER, 
	"LEXICAL_RES_FREQUENCY" FLOAT(126), 
	 CONSTRAINT "CHECK_SENT_HOPE" CHECK (sentisense BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "WORD_PK_HOPE" PRIMARY KEY ("WORD") ENABLE
   ) ;CREATE TABLE  "JOY" 
   (	"WORD" VARCHAR2(20) NOT NULL ENABLE, 
	"EMOSN" NUMBER(1,0), 
	"NRC" NUMBER(1,0), 
	"SENTISENSE" NUMBER(1,0), 
	"FREQUENCY_DATASET" NUMBER, 
	"LEXICAL_RES_FREQUENCY" FLOAT(126), 
	 CONSTRAINT "CHECK_EMOSN_JOY" CHECK (EmoSN BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "CHECK_NRC_JOY" CHECK (NRC BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "CHECK_SENT_JOY" CHECK (sentisense BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "WORD_PK_JOY" PRIMARY KEY ("WORD") ENABLE
   ) ;CREATE TABLE  "LIKE_LOVE" 
   (	"WORD" VARCHAR2(20) NOT NULL ENABLE, 
	"SENTISENSE_LIKE" NUMBER(1,0), 
	"SENTISENSE_LOVE" NUMBER(1,0), 
	"FREQUENCY_DATASET" NUMBER, 
	"LEXICAL_RES_FREQUENCY" FLOAT(126), 
	 CONSTRAINT "CHECK_SENT_LIKE" CHECK (sentisense_like BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "CHECK_SENT_LOVE" CHECK (sentisense_love BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "WORD_PK_LIKELOVE" PRIMARY KEY ("WORD") ENABLE
   ) ;CREATE TABLE  "NEG" 
   (	"WORD" VARCHAR2(20) NOT NULL ENABLE, 
	"GI" NUMBER(1,0), 
	"HL" NUMBER(1,0), 
	"LISTNEGEFFTERMS" NUMBER(1,0), 
	"LIWC" NUMBER(1,0), 
	"FREQUENCY_DATASET" NUMBER, 
	"LEXICAL_RES_FREQUENCY" FLOAT(126), 
	 CONSTRAINT "CHECK_GI" CHECK (GI BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "CHECK_HL" CHECK (HL BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "CHECK_LISTNEG" CHECK (listNegEffTerms BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "CHECK_LIWC" CHECK (LIWC BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "WORD_PK_NEG" PRIMARY KEY ("WORD") ENABLE
   ) ;CREATE TABLE  "NEWWORD" 
   (	"WORD" VARCHAR2(20) NOT NULL ENABLE, 
	"RES" VARCHAR2(20), 
	"FREQUENCY" NUMBER, 
	 CONSTRAINT "NEWWORD_PK" PRIMARY KEY ("WORD") ENABLE
   ) ;CREATE TABLE  "POS" 
   (	"WORD" VARCHAR2(20) NOT NULL ENABLE, 
	"GI" NUMBER(1,0), 
	"HL" NUMBER(1,0), 
	"LISTPOSEFFTERMS" NUMBER(1,0), 
	"LIWC" NUMBER(1,0), 
	"FREQUENCY_DATASET" NUMBER, 
	"LEXICAL_RES_FREQUENCY" FLOAT(126), 
	 CONSTRAINT "CHECK_GIPOS" CHECK (GI BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "CHECK_HLPOS" CHECK (HL BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "CHECK_LISTPOS" CHECK (listPosEffTerms BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "CHECK_LIWCPOS" CHECK (LIWC BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "WORD_PK_POS" PRIMARY KEY ("WORD") ENABLE
   ) ;CREATE TABLE  "SADNESS" 
   (	"WORD" VARCHAR2(20) NOT NULL ENABLE, 
	"NRC" NUMBER(1,0), 
	"SENTISENSE" NUMBER(1,0), 
	"FREQUENCY_DATASET" NUMBER, 
	"LEXICAL_RES_FREQUENCY" FLOAT(126), 
	 CONSTRAINT "CHECK_NRC_SAD" CHECK (NRC BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "CHECK_SENT_SAD" CHECK (sentisense BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "WORD_PK_SAD" PRIMARY KEY ("WORD") ENABLE
   ) ;CREATE TABLE  "SURPRISE" 
   (	"WORD" VARCHAR2(20) NOT NULL ENABLE, 
	"NRC" NUMBER(1,0), 
	"SENTISENSE" NUMBER(1,0), 
	"FREQUENCY_DATASET" NUMBER, 
	"LEXICAL_RES_FREQUENCY" FLOAT(126), 
	 CONSTRAINT "CHECK_NRC_SUR" CHECK (NRC BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "CHECK_SENT_SUR" CHECK (sentisense BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "WORD_PK_SUR" PRIMARY KEY ("WORD") ENABLE
   ) ;CREATE TABLE  "TRUST" 
   (	"WORD" VARCHAR2(20) NOT NULL ENABLE, 
	"NRC" NUMBER(1,0), 
	"FREQUENCY_DATASET" NUMBER, 
	"LEXICAL_RES_FREQUENCY" FLOAT(126), 
	 CONSTRAINT "CHECK_NRC_TRUST" CHECK (NRC BETWEEN 0 and 1) ENABLE, 
	 CONSTRAINT "WORD_PK_TRUST" PRIMARY KEY ("WORD") ENABLE
   ) ;ALTER TABLE  "EMP" ADD FOREIGN KEY ("MGR")
	  REFERENCES  "EMP" ("EMPNO") ENABLE;
CREATE OR REPLACE FUNCTION  "CUSTOM_AUTH" (p_username in VARCHAR2, p_password in VARCHAR2)
return BOOLEAN
is
  l_password varchar2(4000);
  l_stored_password varchar2(4000);
  l_expires_on date;
  l_count number;
begin
-- First, check to see if the user is in the user table
select count(*) into l_count from demo_users where user_name = p_username;
if l_count > 0 then
  -- First, we fetch the stored hashed password & expire date
  select password, expires_on into l_stored_password, l_expires_on
   from demo_users where user_name = p_username;

  -- Next, we check to see if the user's account is expired
  -- If it is, return FALSE
  if l_expires_on > sysdate or l_expires_on is null then

    -- If the account is not expired, we have to apply the custom hash
    -- function to the password
    l_password := custom_hash(p_username, p_password);

    -- Finally, we compare them to see if they are the same and return
    -- either TRUE or FALSE
    if l_password = l_stored_password then
      return true;
    else
      return false;
    end if;
  else
    return false;
  end if;
else
  -- The username provided is not in the DEMO_USERS table
  return false;
end if;
end;
/
/
CREATE OR REPLACE FUNCTION  "CUSTOM_HASH" (p_username in varchar2, p_password in varchar2)
return varchar2
is
  l_password varchar2(4000);
  l_salt varchar2(4000) := 'B7SOWAZG25ADMFMPRHA97LCGO2WFLJ';
begin

-- This function should be wrapped, as the hash algorhythm is exposed here.
-- You can change the value of l_salt or the method of which to call the
-- DBMS_OBFUSCATOIN toolkit, but you much reset all of your passwords
-- if you choose to do this.

l_password := utl_raw.cast_to_raw(dbms_obfuscation_toolkit.md5
  (input_string => p_password || substr(l_salt,10,13) || p_username ||
    substr(l_salt, 4,10)));
return l_password;
end;
/
/
CREATE UNIQUE INDEX  "SYS_C007095" ON  "EMP" ("EMPNO") 
  ;CREATE UNIQUE INDEX  "WORD_PK" ON  "ANGER" ("WORD") 
  ;CREATE UNIQUE INDEX  "WORD_PK_ANT" ON  "ANTICIPATION" ("WORD") 
  ;CREATE UNIQUE INDEX  "WORD_PK_DISHAT" ON  "DISGUST_HATE" ("WORD") 
  ;CREATE UNIQUE INDEX  "WORD_PK_FEAR" ON  "FEAR" ("WORD") 
  ;CREATE UNIQUE INDEX  "WORD_PK_HOPE" ON  "HOPE" ("WORD") 
  ;CREATE UNIQUE INDEX  "WORD_PK_JOY" ON  "JOY" ("WORD") 
  ;CREATE UNIQUE INDEX  "WORD_PK_LIKELOVE" ON  "LIKE_LOVE" ("WORD") 
  ;CREATE UNIQUE INDEX  "WORD_PK_CON" ON  "CONSCORE" ("WORD") 
  ;CREATE UNIQUE INDEX  "WORD_PK_NEG" ON  "NEG" ("WORD") 
  ;CREATE UNIQUE INDEX  "WORD_PK_POS" ON  "POS" ("WORD") 
  ;CREATE UNIQUE INDEX  "WORD_PK_SAD" ON  "SADNESS" ("WORD") 
  ;CREATE UNIQUE INDEX  "WORD_PK_SUR" ON  "SURPRISE" ("WORD") 
  ;CREATE UNIQUE INDEX  "WORD_PK_TRUST" ON  "TRUST" ("WORD") 
  ;CREATE UNIQUE INDEX  "NEWWORD_PK" ON  "NEWWORD" ("WORD") 
  ;
Rem No package found to generate DDL.
Rem No procedure found to generate DDL.
 CREATE SEQUENCE   "DEMO_CUST_SEQ"  MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 21 CACHE 20 NOORDER  NOCYCLE ; CREATE SEQUENCE   "DEMO_ORDER_ITEMS_SEQ"  MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 61 CACHE 20 NOORDER  NOCYCLE ; CREATE SEQUENCE   "DEMO_ORD_SEQ"  MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 11 CACHE 20 NOORDER  NOCYCLE ; CREATE SEQUENCE   "DEMO_PROD_SEQ"  MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 21 CACHE 20 NOORDER  NOCYCLE ; CREATE SEQUENCE   "DEMO_USERS_SEQ"  MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 21 CACHE 20 NOORDER  NOCYCLE ;
Rem No synonym found to generate DDL.
Rem No trigger found to generate DDL.
Rem No view found to generate DDL.
Rem No database link found to generate DDL.
Rem No type found to generate DDL.
Rem No materialized view found to generate DDL.